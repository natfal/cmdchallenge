# CATERPILLAR

## Challenge Text:

```
List names of all the files in the current directory, one file per line.
```
## Solution:

`A` all entries except . and ..\
`1` one file per line

```
$ ls -A1
```